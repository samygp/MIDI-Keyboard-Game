using UnityEngine;
using System.Collections;

public class InputBehaviour : MonoBehaviour {

    // Use this for initialization
    void Start(){
    }

    // Update is called once per frame
   /* void Update () {/*
        if (Input.anyKeyDown){
            BroadcastMessage("CheckNote", Input.inputString);
            Debug.Log(Input.inputString);
        }
    }*/
}
